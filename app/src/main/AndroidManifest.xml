<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="br.com.fiap25mob.mbamobile">

    <application
        android:name="br.com.fiap25mob.mbamobile.utils.App"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:enableOnBackInvokedCallback="true"
        android:theme="@style/Theme.MbaMobile"
        tools:targetApi="tiramisu">
        <activity
            android:name="br.com.fiap25mob.mbamobile.presentation.RegisterActivity"
            android:exported="false" />
        <activity
            android:name="br.com.fiap25mob.mbamobile.presentation.LoginActivity"
            android:exported="true"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="br.com.fiap25mob.mbamobile.presentation.MainActivity"
            android:exported="true" />
    </application>

</manifest>